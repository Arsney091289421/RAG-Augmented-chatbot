
services:
  rag-chatbot:
    build: .
    container_name: rag-chatbot
    ports:
      - "5050:5050"
    env_file:
      - .env
    volumes:
      - ./embeddings:/app/docs_clean/embeddings
    restart: unless-stopped
