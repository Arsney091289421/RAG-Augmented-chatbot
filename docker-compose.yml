version: '3.8'

services:
  rag-chatbot:
    build: .
    container_name: rag-chatbot
    ports:
      - "5050:5050"
    env_file:
      - .env
    volumes:
      - ./embeddings:/app/embeddings
    restart: unless-stopped
